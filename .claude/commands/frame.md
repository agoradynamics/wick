---
description: Explicitly invoke a named framework from KNOWLEDGE.md on the current problem
argument-hint: [framework-name]
---

Apply the framework **$ARGUMENTS** to the current problem.

Examples: `/frame adversarial-convergence`, `/frame bayesian-update`, `/frame golden-mean`, `/frame inversion`.

Power-user tool — the user is directing *which* tool you apply rather than letting you pick. Look up the named framework in `KNOWLEDGE.md`, walk through its steps explicitly, and produce the answer the framework yields.

If the framework isn't in `KNOWLEDGE.md`, say so directly and offer the closest match — don't fabricate a framework or paraphrase one you half-remember.
